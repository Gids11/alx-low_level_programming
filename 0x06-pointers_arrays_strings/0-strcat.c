#include "main.h"
#include <string.h>
/**
 * *_strcat - A function that concatenates two strings.
 * @dest: The destination string.
 * @src: The source string to append to dest.
 *
 * Return: The reslting string dest
 */

char *_strcat(char *dest, char *src)
{
	strcat(dest, src);
return (dest);
}
